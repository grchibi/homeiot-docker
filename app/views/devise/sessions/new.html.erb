<div class="bs-docs-section">
  <div class="row">
    <div class="col-lg-12">
            
      <div class="page-header">
        <div class="row">
          <div class"col-md-12">
            <h1><%= t('views.devise.sessions.new.login_title') %></h1>
          </div>
        </div>
      </div>  <!-- page-header -->

      <div class="row">
        <div class="col-lg-4">
    
<%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "form-horizontal"}) do |f| %>
  <filedset>
    
    <div class="form-group">
      <%= f.label t('views.devise.sessions.new.email'), {class: 'control-label'} %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label t('views.devise.sessions.new.pwd'), {class: 'control-label'} %><br />
      <%= f.password_field :password, autocomplete: "current-password", class: 'form-control' %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="form-group">
        <%= f.check_box :remember_me %>
        <%= f.label t('views.devise.sessions.new.remember_me') %>
      </div>
    <% end %>

    <div class="form-group actions">
      <%= f.submit t('views.devise.sessions.new.login'), :class => 'btn btn-primary' %>
    </div>
    
  </filedset>
<% end %>

          <%= render "devise/shared/links" %>

        </div>  <!-- col-lg-4 -->
      </div>  <!-- row -->

    </div>  <!-- col-lg-12 -->
  </div>  <!-- row -->
</div>  <!-- bs-docs-section -->
      